import { Card, GamePlayer } from "@prisma/client";

const NUMBER_OF_CARDS_IN_DECK = 15;

const IMMUTABLE_CARDS = {
    DUKE: 3,
    CONTESSA: 3,
    ASSASSIN: 3,
    CAPTAIN: 3,
    AMBASSADOR: 3,
};

const dealCards = (players: Array<GamePlayer>, cards: Array<Card>) => {


    return;
};

export default dealCards;
